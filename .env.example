SPRING_MONGODB_URI=mongodb+srv://<usuario>:<password_url_encoded>@<cluster>.mongodb.net/<database>?appName=<app>
# Secreto de firma JWT (HS256, mínimo 32 caracteres / 256 bits). Generar uno propio, p. ej.:
#   openssl rand -base64 48
JWT_SECRET=<clave_secreta_larga_de_al_menos_32_caracteres>
